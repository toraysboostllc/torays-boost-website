/**
 * FAQ display order only — the question/answer text itself lives in
 * src/i18n/translations.js (faq.items.<id>.question/answer, in both
 * languages) so there is exactly one place to edit FAQ copy.
 */
export const faqOrder = ["warranty", "turnaround", "diagnostics", "shipping", "data-safety", "unrepairable"];
